// 1. Create a module that exports a function to generate random numbers between two given numbers.

// 2. Create a module that exports a function to calculate the area of a circle, given its radius.

// 3. Create a module that exports an object with properties for converting different units of measurement (e.g. length, weight, volume).

// units.js 
// length:
//         metersToFeet
//         feetToMeters

// weight:
        // kilogramsToPounds
        // poundsToKilograms

// volume:
        // litersToGallons
        // gallonsToLiters

// main.js 
units.length.metersToFeet(10) // result should be in feet

// 4. Create a module that exports a class to represent a bank account, with methods for depositing, withdrawing, and checking the balance.

// bank.js 

// deposit
// withdraw
// getBalance

// main.js
myAccount.deposit(10000)
console.log(myAccount.getBalance())

myAccount.withdraw(10000)
console.log(myAccount.getBalance())

// 5. Create a module that exports a function to generate a random password with a given length and complexity level (e.g. lowercase, uppercase, numbers, symbols).

// 6. Create a module that exports a function to check if a given number is prime or not.

// 7. Create a module that exports a function to calculate the factorial of a given number.

// 8. Create a module that exports a function to sort an array of objects by a given property.

// 9. Create a module that exports a function to convert a given string to title case (i.e. capitalize the first letter of each word).